var el = document.getElementById('<%= dom_id(@book, :collapse) %>')
el.outerHTML = '<%= j render(partial: 'books/collapse', object: @book, as: :book, locals: { show: true }) %>'

$('#<%= @book.dom_id %>')
  .find('.floating-label .custom-select, .floating-label .form-control')
  .floatinglabel();
