var elements = document.getElementById('<%= @book.dom_id %>').querySelectorAll('.vote');

elements.forEach((el) => {
  el.outerHTML = '<%= j render(partial: 'books/vote', object: @book, as: :book) %>';
})
